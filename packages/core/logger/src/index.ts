/* tslint:disable:no-console */
const logError = (error: string) => console.error(error)

const logInfo = (value: string) => console.info(value)

const logWarn = (value: string) => console.warn(value)
/* tslint:disable:no-console */

export { logError, logInfo, logWarn, }
